# Subphase 008.2: Remediation for Phase 008.1 (notes still report inaccurate eslint-warning metrics)

## Context
This subphase was generated by the phase verifier after reviewing
`docs/RFC/ToDo/issue-118/000-initial-work/phases/phase-008.1.md` (the remediation of Phase 008).

**The functional implementation and the two load-bearing fixes from 008.1 are verified correct.**
Specifically, the verifier independently confirmed:

- **Issue 2 (the real fix) — PASS.** The two new Phase 008 `artifact_stored` test blocks in
  `tests/unit/services/event-stream.test.ts` are now guarded (`const es = mockInstances[0]!;` at
  lines 223 and 252). `svelte-check` reports exactly **14 errors** (net-new = 0 vs the HEAD baseline of
  14). None of the 14 errors fall on the new `artifact_stored` blocks — they are all in the pre-existing
  first `EventStreamManager` describe block (lines 40–153) and `renderers/McpApp.test.ts:86`. The diff of
  `event-stream.test.ts` vs HEAD is purely additive (only the new describe block was appended); the
  pre-existing 14 were NOT mass-refactored (scope discipline upheld). `eslint .` exits 0.
- **Issue 1 (build artifact — confirm only) — PASS / informational.** Working-tree `dist/index.html`
  references the untracked entry chunk `assets/index-CHCoV4W0.js`, which contains
  `penguiflow_ui_component` (1 occurrence). `npm run build` is idempotent (re-running produced the same
  chunk hash, no new churn). Committed `HEAD:dist/index.html` still points at the stale, handler-less
  `index-DwaEH6PZ.js` — left for integration-time commit, as intended. No commit was made.
- **Tests — PASS.** `npx vitest run` -> **435 passed** (40 files), 0 failures. The guarded accesses did
  not weaken behavior: the `penguiflow_ui_component` test still asserts the fetch URL/headers, that
  `addArtifactChunk` is called exactly once with `artifact_type === 'ui_component'` /
  `meta.artifact_id === 'artifact-1'` / `options === { message_id: 'msg-7' }`, and that the
  download-only `artifactsStore.addArtifact` is NOT called; the binary test still asserts the
  download path with no JSON fetch.
- **Build — PASS.** `npm run build` (vite) exits 0.

**The single remaining defect is Issue 3 (notes accuracy).** Subphase 008.1's whole purpose for Issue 3
was to replace inaccurate lint metrics in `phase-008.md` with *factual* ones. Instead it replaced them
with **different inaccurate numbers**. Every eslint-warning figure the corrected notes now state is wrong.

## Parent Phase
- File: `docs/RFC/ToDo/issue-118/000-initial-work/phases/phase-008.1.md`
- Grandparent: `docs/RFC/ToDo/issue-118/000-initial-work/phases/phase-008.md`
- Original Objective (008): store-backed `artifact_stored` UI-component rendering + fetch-by-id + dedupe.
- 008.1 Objective: guard the 2 new svelte-check errors (done), confirm dist consistency (done), and
  **correct the inaccurate Phase 008 lint metrics to factual ones** (NOT done correctly).

## Issues to Resolve

### Issue 1: The corrected eslint-warning metrics in `phase-008.md` are factually wrong (all four numbers)
- **Type**: Incorrect Implementation (misreported metric — the very thing 008.1 Issue 3 was meant to fix)
- **Location**:
  - `docs/RFC/ToDo/issue-118/000-initial-work/phases/phase-008.md`:
    - Line 211: "`session-stream.ts` went from 2 -> 12 warnings and `interactions.svelte.ts` from 2 -> 8 warnings"
    - Line 257: "net-new advisory warnings are **not** 0 — `session-stream.ts` 2 -> 12, `interactions.svelte.ts` 2 -> 8"
  - `docs/RFC/ToDo/issue-118/000-initial-work/phases/phase-008.1.md` (echoes the same wrong numbers):
    - Lines 106–107, 113, 182, 192–193 (the "2 -> 12" / "2 -> 8" and "+10 / +6" claims and the
      "verified by running `npx eslint`" assertion).
- **Description**: The verifier measured the actual eslint warning counts directly
  (`npx eslint <file>`), both on the pristine HEAD checkout (full `git stash -u`) and on the working
  tree. The real numbers are:

  | File | HEAD baseline (claimed) | HEAD baseline (actual) | Working tree (claimed) | Working tree (actual) | Net-new (claimed) | Net-new (actual) |
  |------|-------------------------|------------------------|------------------------|-----------------------|-------------------|------------------|
  | `src/lib/services/session-stream.ts` | 2 | **10** | 12 | **11** | +10 | **+1** |
  | `src/lib/stores/features/interactions.svelte.ts` | 2 | **7** | 8 | **7** | +6 | **0** |

  The likely root cause of the working-tree error: the implementer counted with `grep -c warning`
  (or similar), which also matched the trailing eslint summary line ("✖ N problems (0 errors, N
  warnings)"), inflating each count by exactly 1 (11->12, 7->8). The HEAD baseline of "2" is simply
  wrong — both files already carried many advisory warnings before Phase 008 (10 and 7 respectively),
  so the real net-new is +1 for `session-stream.ts` and **0** for `interactions.svelte.ts`, not
  "+10 / +6".
- **Expected Behavior**: The Phase 008 Implementation Notes state the true eslint warning counts:
  `session-stream.ts` 10 -> 11 (net-new +1), `interactions.svelte.ts` 7 -> 7 (net-new 0). The narrative
  that `eslint .` still exits 0 (warn-only rules) remains correct and should be kept.
- **Current Behavior**: The notes assert "2 -> 12" and "2 -> 8" (net-new +10 / +6), none of which match
  reality. The 008.1 notes additionally claim these were "verified by running `npx eslint` on each file
  in the working tree: `session-stream.ts` = 12 warnings, `interactions.svelte.ts` = 8 warnings" — that
  verification statement is itself inaccurate.
- **Suggested Approach**: Documentation-only. Re-measure with a method that excludes the summary line —
  e.g. `npx eslint <file> -f json | jq '.[0].warningCount'`, or
  `npx eslint <file> | grep -cE '^[[:space:]]+[0-9]+:[0-9]+[[:space:]]+warning'` — for both the
  pristine HEAD (via `git stash -u`) and the working tree, then correct the four numbers (and the
  net-new deltas) in `phase-008.md` lines 211 & 257 and the mirrored lines in `phase-008.1.md`. Do NOT
  change any source code to "make the numbers true"; the advisory warn-only rules are explicitly out of
  scope and `eslint .` already exits 0. This is purely a metric-accuracy correction.

## Exit Criteria
- [ ] `phase-008.md` Implementation Notes state the actual eslint warning counts for both files
      (`session-stream.ts` 10 -> 11 / net-new +1; `interactions.svelte.ts` 7 -> 7 / net-new 0), or
      whatever the re-measured values are at correction time, measured with a method that does not
      count the eslint summary line.
- [ ] `phase-008.1.md` notes are reconciled to the same accurate numbers (no lingering "2 -> 12" /
      "2 -> 8" / "+10" / "+6" claims).
- [ ] The verification can be reproduced: `npx eslint <file> -f json | jq '.[0].warningCount'` on the
      working tree returns the numbers stated in the notes; the same command on a clean
      `git stash -u` checkout returns the stated HEAD baselines.
- [ ] No source/test/build changes are made for this subphase (notes-only). `npm test` stays at 435
      passing and `svelte-check` stays at 14 (net-new 0) — i.e. the 008.1 fixes are untouched.

## Files Likely to be Modified
- `docs/RFC/ToDo/issue-118/000-initial-work/phases/phase-008.md` (correct lines 211 & 257)
- `docs/RFC/ToDo/issue-118/000-initial-work/phases/phase-008.1.md` (reconcile the mirrored counts at
  lines ~106–107, 113, 182, 192–193)

---

## Implementation Notes

**Implemented by:** phase-implementer agent
**Date:** 2026-06-26

### Summary of Changes

This is a documentation-only remediation. No source, test, or build/dist files were modified. Only the
eslint-warning metrics in the Phase 008 and Phase 008.1 Implementation Notes were corrected to match
authoritatively re-measured values.

- **Re-measured eslint warning counts authoritatively** using the JSON formatter (which excludes the
  trailing eslint summary line), from `penguiflow/cli/playground_ui/`:
  - `npx eslint -f json src/lib/services/session-stream.ts | jq '.[0].warningCount'`
  - `npx eslint -f json src/lib/stores/features/interactions.svelte.ts | jq '.[0].warningCount'`
- **Measured the HEAD baseline** by stashing the entire working tree (`git stash push -u`, which produced a
  clean checkout at HEAD `c8636034`), running the same two `jq '.[0].warningCount'` commands, then
  `git stash pop` to restore the working tree exactly. The working-tree restoration was verified byte-for-byte
  (porcelain status identical, tracked-diff hash `5edb8d4f…` unchanged, 168-file change set unchanged,
  3 pre-existing stashes intact).
- **Corrected `phase-008.md`** (2 edits): the "Deviations from Plan" eslint-warning bullet and the
  "Verification Results" eslint bullet.
- **Corrected `phase-008.1.md`** (4 edits): the Issue 3 Location bullet, the Issue 3 Current Behavior bullet,
  the "Issue 3 corrections made to phase-008.md" notes bullet, and the "verified by running npx eslint"
  paragraph in the Implementation Notes.

### Authoritative Measured Numbers (via `npx eslint -f json | jq '.[0].warningCount'`)

| File | HEAD baseline (clean stash) | Working tree | Net-new |
|------|-----------------------------|--------------|---------|
| `src/lib/services/session-stream.ts` | **10** | **11** | **+1** |
| `src/lib/stores/features/interactions.svelte.ts` | **7** | **7** | **0** |

- These replace the inaccurate "2 -> 12" / "2 -> 8" (net-new "+10 / +6") figures that subphase 008.1 had
  written into both phase files (Issue 1 of this subphase). Note: subphase 008.1 was *itself* "correcting"
  Phase 008's original (also-wrong) "net new warnings: 0" claim — so both prior figures were wrong, and this
  subphase replaces them with the true counts.
- Root cause of the prior "+10 / +6": the count was taken with `grep -c warning` (which also matches the
  trailing eslint summary line `✖ N problems (0 errors, N warnings)`, inflating each count by exactly 1:
  11 -> 12, 7 -> 8), combined with a fabricated "2" HEAD baseline (both files already carried 10 and 7
  advisory warnings before Phase 008). The `jq '.[0].warningCount'` value reads the structured `warningCount`
  field directly and is not affected by the summary line.

### svelte-check baseline (re-confirmed)

- On the clean HEAD stash, `npx svelte-check --output machine` reported `COMPLETED 784 FILES 14 ERRORS 0
  WARNINGS 2 FILES_WITH_PROBLEMS` — i.e. the **14-error** baseline. This matches the verifier's
  "14 errors, net-new 0" finding, so the svelte-check numbers already in the phase notes are accurate and
  were left unchanged.

### Exact lines corrected

`phase-008.md`:
- "Deviations from Plan" `asPlainRecord` bullet — replaced "`session-stream.ts` went from 2 -> 12 warnings
  and `interactions.svelte.ts` from 2 -> 8 warnings" with "`session-stream.ts` 10 -> 11 (net-new +1) /
  `interactions.svelte.ts` 7 -> 7 (net-new 0)", and added the `jq '.[0].warningCount']` measurement method
  plus the root-cause note.
- "Verification Results" eslint bullet — replaced "net-new advisory warnings are **not** 0 —
  `session-stream.ts` 2 -> 12, `interactions.svelte.ts` 2 -> 8" with "10 -> 11 (net-new +1) / 7 -> 7
  (net-new 0)".

`phase-008.1.md`:
- Issue 3 → "Location" — "`session-stream.ts` (2 -> 12 warnings) and `interactions.svelte.ts` (2 -> 8
  warnings)" corrected to "10 -> 11 / 7 -> 7" with the JSON-formatter method noted.
- Issue 3 → "Current Behavior" — "+10 warnings in `session-stream.ts`, +6 in `interactions.svelte.ts`"
  corrected to "+1 (10 -> 11) / 0 (7 -> 7)".
- Implementation Notes → "Issue 3 corrections" bullet — "`session-stream.ts` 2 -> 12,
  `interactions.svelte.ts` 2 -> 8" corrected to "10 -> 11 / net-new +1, 7 -> 7 / net-new 0".
- Implementation Notes → "verified by running `npx eslint`" paragraph — "`session-stream.ts` = 12 warnings,
  `interactions.svelte.ts` = 8 warnings" corrected to the JSON-formatter working-tree values (11 / 7) and
  HEAD values (10 / 7), with the summary-line caveat.

The remaining occurrences of the strings "2 -> 12 / 2 -> 8 / +10 / +6" in both files are intentional: they
appear only inside explanatory clauses that quote the *old* figures explicitly to flag them as inaccurate
(e.g. "the +10/+6 figures came from counting with `grep -c warning`"). They are not asserted as true.

### Key Considerations

- Used `npx eslint -f json | jq '.[0].warningCount'` as the single source of truth per the subphase's
  suggested approach, because it reports the structured per-file `warningCount` and is immune to the
  summary-line over-count that caused the original error.
- Chose `git stash push -u` / `git stash pop` (rather than a temporary worktree) for the HEAD baseline,
  because the worktree would lack `node_modules` and could resolve a different eslint config/plugin context;
  stashing measures the same checkout with the same installed toolchain, isolating only the file-content
  difference. Took a porcelain snapshot and the tracked-diff hash before stashing and re-verified both after
  popping, plus confirmed the pre-existing 3 stashes were untouched, so there was no risk to the in-progress
  Phase 008 working-tree changes.
- Did not change any source to "make the numbers true" — the flagged rules (`complexity`, `max-depth`,
  `explicit-function-return-type`, `max-lines-per-function`) are warn-only and `eslint .` already exits 0;
  the subphase explicitly puts them out of scope.

### Assumptions

- The `jq '.[0].warningCount'` value is the authoritative warning count for a single-file lint run (the
  eslint JSON output is a one-element array when a single file is passed; confirmed `.[0]` is the target file).
- The advisory warn-only rules and the 14 pre-existing svelte-check errors remain acceptable / out of scope,
  consistent with the subphase ("This is purely a metric-accuracy correction").
- The HEAD baseline measured at commit `c8636034` (current branch tip) is the correct "pre-phase" reference,
  matching the verifier's stated baseline.

### Deviations from Plan

- None. The corrections are documentation-only and match the subphase's verified-correct values exactly
  (session-stream.ts 10 -> 11 / +1; interactions.svelte.ts 7 -> 7 / 0; svelte-check 14 / net-new 0).

### Potential Risks & Reviewer Attention Points

- None functional. No source/test/dist files were touched; the entire Phase 008 functional change set
  (source + tests + rebuilt `dist/`) is byte-for-byte intact (verified via identical porcelain status and the
  unchanged tracked-diff hash after the stash round-trip).
- The numbers are reproducible: from `penguiflow/cli/playground_ui/`, run
  `npx eslint -f json <file> | jq '.[0].warningCount'` on the working tree (-> 11 / 7) and, after
  `git stash push -u`, on the clean HEAD checkout (-> 10 / 7), then `git stash pop`.

### Files Modified

- `docs/RFC/ToDo/issue-118/000-initial-work/phases/phase-008.md` (2 metric corrections)
- `docs/RFC/ToDo/issue-118/000-initial-work/phases/phase-008.1.md` (4 metric corrections)
- `docs/RFC/ToDo/issue-118/000-initial-work/phases/phase-008.2.md` (this Implementation Notes section)

### Verification Results

- Working tree (authoritative, `jq '.[0].warningCount'`): `session-stream.ts` = **11**,
  `interactions.svelte.ts` = **7**.
- HEAD baseline (clean `git stash -u` checkout, same command): `session-stream.ts` = **10**,
  `interactions.svelte.ts` = **7**. Net-new: **+1** / **0**.
- svelte-check at HEAD: **14 errors** (`COMPLETED 784 FILES 14 ERRORS 0 WARNINGS`), net-new 0 — unchanged.
- `git status` after edits: only the phase markdown files changed; the pre-existing 168-file Phase 008
  working-tree change set (source, tests, `dist/`) is fully preserved and untouched.
