# ToolNode MCP Presets

Quick peek at the built-in MCP presets for `ToolNode`.

## Run

```bash
uv run python examples/toolnode_presets/flow.py
```

No external services are called; this simply prints the preset configs.
